logging:
  level:
    com:
      mimiter:
        mgs:
          core:
            repository: debug
            service: debug

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mgs_test?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Hongkong

grpc:
  client:
    myService:
      address: static://106.14.42.183:5555
